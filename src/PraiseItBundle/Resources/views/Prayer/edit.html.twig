{% extends 'PraiseItBundle:Paladin:base.html.twig' %}

{% block results -%}
    <h1>Prayer edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('prayer') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
